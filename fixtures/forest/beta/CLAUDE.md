# Coldframe Notes

Coldframe Notes is a made-up gardening-log project. It turns a season's worth of hand-written planting
notes into a searchable index, grouped by bed, crop, and frost date, so a small volunteer crew can plan
next year's rotation without re-reading a paper notebook.

The project has no server component; everything runs as a local batch job over a folder of text files.
