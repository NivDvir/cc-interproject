# Vendored Interpreter Package

This file exists only to prove that a `CLAUDE.md` inside a `.venv` is skipped as vendored material
rather than treated as a project belonging to Coldframe Notes.

Any test that finds this file as a discovered project has a broken skip list.
