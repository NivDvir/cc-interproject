# Dot-Claude Home

This file stands in for the `CLAUDE.md` that lives directly under a user's `~/.claude` directory. It
is made-up content describing a fictional coordination note, used only to give the fallback row
extractor a heading and a paragraph to read when no other signal is available.

It should be discovered like any other top-level project root during a forest walk.
