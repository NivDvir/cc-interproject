# Vendored Widget Package

This file exists only to prove that a `CLAUDE.md` inside `node_modules` is treated as vendored
material and skipped by the forest walk, never as a project of its own.

If a scan ever lists this file as a discovered project, the skip list is broken.
