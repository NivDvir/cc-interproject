# Meridian Logbook — Tidal Module

The tidal module is a subtree of Meridian Logbook. It converts tide-table CSVs into the same
conflict-window format the parent project consumes, so port windows can be checked against low tide
without a separate pipeline.

It has no independent release process; it ships whenever the parent project ships.
