# Deeply Nested Project

A real project that happens to sit nine directories below the home directory, inside a
folder tree someone built while sorting an old backup.

The forest walk stops at depth six, so this project is not discovered. That is the
documented behaviour: a user who keeps projects this deep registers them by hand.
