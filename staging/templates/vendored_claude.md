# Vendored Dependency

This file ships inside a third-party package that was installed into this machine by a
package manager. It is not a project of this user's.

A scanner that offers this directory as a project to register has failed: the user does
not own it, cannot edit it, and it will be deleted the next time the lockfile changes.
