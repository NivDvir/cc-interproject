"""Builds, writes, loads, and interprets the kit's install manifest
(`~/.claude/interproject.manifest.json`), and the uninstall / status operations that read it back.
"""

from __future__ import annotations
